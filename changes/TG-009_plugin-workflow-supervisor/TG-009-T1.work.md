# Worklog: T1 - Workflow contracts and validation

**Unit:** T1
**Route:** PROCEED
**Status:** done

## Plan

- Add a `workflow` vertical slice that exposes the v1 template, plugin, request, and result contract types.
- Keep file reads and canonical containment checks in the loader edge; perform exact scalar substitution and graph/context validation as value-in/value-out steps before any future Store write can occur.
- Pin the contract with RED-first table-driven tests for malformed schemas, input matching, path escape, hierarchy/dependency invariants, shared-context ownership, literal argv, and request/result parsing.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) - VERIFIED - `src/lib.rs:3-8` has no workflow module, and no `src/workflow.rs` or workflow tests exist.
- **PB-SENSE** (still makes sense) - VERIFIED - T1 is unchecked at `CHANGE.md:44`; S1's reusable validated template outcome at `CHANGE.md:19` remains unmet.
- **PB-PLAN** (plan achieves impact, in bounds) - VERIFIED - `DESIGN.md:82-180` specifies the complete v1 contracts, while `DESIGN.md:241-250` assigns parsing to `src/workflow/template.rs` and defers persistence, running, status, and CLI routing to later slices.
- **PB-CONTRACT** (contract/types still sound) - VERIFIED - strict serde contract types plus validation encode Decision-3's version, node-kind, graph, context, input, and argv invariants; expected failures use the repository's `TgError::InvalidInput` convention from `src/errors.rs:28-29`.
- **PB-SHAPE** (service shape sound) - VERIFIED - `src/store/root.rs:9-25` supplies root discovery independently; T1 can isolate canonicalization/file reads at one loader boundary and keep substitution plus graph validation pure without Store/process dependencies.

## Prevention / diff self-check

- RED: `cargo test --test workflow_template_test` failed before implementation because `task_golem::workflow` did not exist (E0433), pinning the new public contract surface.
- GREEN: `cargo test --test workflow_template_test` passed 5/5 tests covering strict contracts, exact substitution/input matching, graph/context failures, contained canonical paths, and literal argv.
- Full verify: `just check` passed `cargo fmt --check`, `cargo clippy -- -D warnings`, all unit/integration tests, and doc tests; existing integration test utility dead-code warnings remain non-failing.
- Commit partition verify: sequential temporary-worktree `just check` runs passed after planning artifacts (379 lines), implementation (737 lines), and tests/worklog (505 lines); every suggested commit is independently green and below the 900-line limit.
- Exact diff reread: scope contains only workflow contracts/parser/validation, tests, and required work artifacts; T1 remains unchecked. No Store writes, CLI routing, runner/status behavior, fixture plugins, unsafe code, source comments, debug output, or TODO/FIXME markers were added.
- Simplicity/safety: file I/O is limited to canonicalized contained definition reads, validation is pure after loading, expected failures use `TgError::InvalidInput`, and one cycle traversal serves both parent and dependency graphs. `git diff --cached --check` passed.

## Review ledger

### Gate 2 attempt 2026-08-13

**Scope/code fingerprint:** `sha256:3775ab29b9bd387b2a1897761cd90d3c2bb1dfdba9a2f3bfae96f6cf4b5fa519` (pre-repair staged T1 code/tests)
**Task acceptance:** `CHANGE.md` T1 / S1 template, plugin, request, and result contracts; exact non-argv scalar substitution; graph/context validation; contained definition paths; no writes
**Exact diff:** `git diff --cached -- src/lib.rs src/workflow.rs src/workflow/template.rs tests/workflow_template_test.rs` at the fingerprint above
**Gate 1 evidence:** PASS - the orchestrator reported that this exact staged unit passed Gate 1; the worklog's earlier full `just check` evidence records exit-success formatting, Clippy, tests, and doc tests
**Coverage:** detector coverage COMPLETE 4/4 · specialists none triggered · adjudicator COMPLETE (fresh)
**Review state:** NOT REVIEWED - validated repairs are applied and locally verified but await fresh re-adjudication

| candidate ID | source | location / claim | severity / confidence | evidence checked | disposition | action | rationale | duplicate_of | fingerprint | repair state |
|---|---|---|---|---|---|---|---|---|---|---|
| COR-1 | correctness detector | `validate_acyclic`: recursive DFS can overflow on a valid 50,000-node graph | Medium / High | recursive `visit`; parent/dependency callers; deep-chain behavior | VALIDATED | AUTO-FIX | Input size is unbounded and recursion depth follows graph depth; an explicit stack preserves cycle validation without consuming the call stack. | N/A | `3775ab29` | verified by focused test and full `just check`; re-adjudication pending |
| COR-2 | correctness detector | `parse_plugin_result`: add context-aware result/session validation | Low / High | `DESIGN.md` Plugin v1 rules; parser contract; runner/context work deferred beyond T1 | REJECTED | DROP | Plugin results intentionally carry no request context, so context-dependent interpretation belongs to the later runner and changing parsing would exceed the authorized T1 contract. | N/A | `3775ab29` | none |
| COR-3 | correctness detector | `index_nodes`: workflow titles permit multiline values rejected by TG items | High / High | `Item::validate_title`; node normalization path; multiline fixture | VALIDATED | BLOCK | Instantiation will create ordinary TG items, so accepting a title that TG itself rejects violates the shared item invariant. | N/A | `3775ab29` | verified by multiline regression and full `just check`; re-adjudication pending |
| CON-1 | contract detector | public contract derives: remove `Deserialize` implementations | Low / High | serde use sites for template enums and plugin request/result contracts | REJECTED | DROP | Deserialization is required by the declared YAML/JSON input contracts; removing derives is not a correctness repair. | N/A | `3775ab29` | none |
| CON-2 | contract detector | `CHANGE.md` Decision-10 / `DESIGN.md` contract: revise the authorized v1 contract | Low / High | authorized RFC markers; Decision-10; Design Template/Plugin rules | REJECTED | DROP | T1 must implement the authorized exact-scalar and literal-argv boundary, not supersede it during a repair cycle. | N/A | `3775ab29` | none |
| CON-3 | contract detector | input substitution: mapping keys are skipped and `verify` argv is rewritten | High / High | substitution walkers; Decision-10; `DESIGN.md:121-125`; plugin loading boundary | VALIDATED | BLOCK | Exact placeholders apply to YAML mapping-key scalars, while both plugin and verify argv are authoritative literals; post-substitution duplicate keys must fail before deserialization/writes. | N/A | `3775ab29` | verified by key replacement/collision, nested scalar, literal argv regressions, and full `just check`; re-adjudication pending |
| TST-001 | test detector | missing mapping-key, collision, and literal verify coverage | High / High | existing substitution test and CON-3 evidence | DUPLICATE | NONE | This is the test-coverage manifestation of CON-3 and is repaired by the same behavior-level cases. | CON-3 | `3775ab29` | verified by focused test and full `just check`; re-adjudication pending |
| SIM-1 | simplicity detector | remove `Deserialize` derives as dead surface | Low / High | derive consumers and CON-1 evidence | DUPLICATE | NONE | Same proposed derive removal as CON-1; the derives are consumed by contract parsing. | CON-1 | `3775ab29` | none |
| SIM-2 | simplicity detector | fold context-aware result semantics into T1 parsing | Low / High | parser boundary, deferred runner scope, and COR-2 evidence | DUPLICATE | NONE | Same out-of-scope parser expansion as COR-2; no simpler in-contract repair results. | COR-2 | `3775ab29` | none |

### Review return

- **Validated:** 3 - COR-1, COR-3, CON-3.
- **Cleared (rejected/duplicate):** 6 - COR-2, CON-1, CON-2 rejected; TST-001, SIM-1, SIM-2 deduplicated.
- **Needs evidence:** 0.
- **Repairs:** iterative explicit-stack cycle validation; TG title invariant reuse; mapping-key substitution with duplicate collision errors and literal plugin/verify argv; focused 7/7 and full `just check` pass.
- **Coverage:** COMPLETE 4/4 detectors; fresh adjudicator complete; post-repair re-adjudication remains pending.
- **Updated partition verify:** planning artifacts remain unchanged from their prior green check (379 lines); a fresh detached-worktree `just check` passed for planning plus corrected implementation (802 implementation lines); the final tests/worklog partition is covered by the full staged `just check` pass (629 lines). `GREEN_PER_COMMIT: yes`.

### Gate 2 attempt 2026-08-13 (second historical attempt)

**Scope/code fingerprint:** `sha256:29f8146180818572a3ba83c0f7c0146204e256671f6f7f477732d7b6798e4c8b` (pre-repair staged T1 code/tests)
**Post-repair code fingerprint:** `sha256:2d2cfaa207e51ef9de50b65ba2fc60df6833b70ea16af38aebec614baebeda9c` (corrected T1 code/tests)
**Task acceptance:** `CHANGE.md` T1 / S1 template, plugin, request, and result contracts; exact non-argv scalar substitution; graph/context validation; contained definition paths; no writes
**Exact diff:** `git diff --cached -- src/lib.rs src/workflow.rs src/workflow/template.rs tests/workflow_template_test.rs` at the pre-repair fingerprint; the same path set at the post-repair fingerprint above
**Gate 1 evidence:** PASS - the orchestrator reported Gate 1 green for the exact pre-repair staged unit
**Coverage:** detector coverage COMPLETE 4/4 - correctness detector returned no candidates; contract, test, and simplicity detectors completed; adjudicator COMPLETE (fresh)
**Review state:** NOT REVIEWED - this attempt's terminal adjudication required repairs; all validated actions are applied and locally verified, pending fresh re-adjudication

| candidate ID | source | location / claim | severity / confidence | evidence checked | disposition | terminal action | rationale | duplicate_of | fingerprint | repair state |
|---|---|---|---|---|---|---|---|---|---|---|
| RCON-1 | contract detector | `validate_argv`: embedded NUL reaches process argv in plugin or verify arguments | High / High | plugin and verify argv loaders; direct process contract; first and subsequent argument cases | VALIDATED | BLOCK | Process argv cannot represent embedded NUL, so every plugin and verify argv element must reject it before execution. | N/A | `29f81461` | APPLIED - shared argv validation rejects NUL in every element; plugin and verify first/subsequent argument regressions pass |
| RCON-2 | contract detector | `validate_argv`: all blank literal tail arguments are rejected with the program | Medium / High | direct argv contract; program position versus literal subsequent arguments | VALIDATED | AUTO_FIX | Only `argv[0]` must identify a nonblank program; empty and whitespace subsequent literals are representable and must remain allowed. | N/A | `29f81461` | APPLIED - nonblank program is required while empty/whitespace tail arguments round-trip for plugin and verify argv |
| RTST-1 | test detector | literal verify coverage does not reuse a key substituted elsewhere | Medium / High | exact-scalar substitution fixture; verify exclusion; isolated plugin argv fixture | VALIDATED | AUTO_FIX | Reusing the title input key proves verify argv remains literal even when the same placeholder is resolved elsewhere in the template. | N/A | `29f81461` | APPLIED - verify retains `${title}` while the Task title resolves from the supplied title input |
| RTST-2 | test detector | request coverage omits valid `none` and resumed shared-without-session rejection | Medium / High | `validate_request_context`; request JSON fixtures; v1 context contract | VALIDATED | AUTO_FIX | Both the valid no-session mode and the resumed shared precondition are public request-contract behavior. | N/A | `29f81461` | APPLIED - valid `none` parses and resumed shared without `session_ref` rejects |
| RTST-3 | test detector | add a symlink path-containment test | Low / Medium | canonical path containment implementation; existing outside-workspace path tests; requested T1 scope | REJECTED | DROP | Canonicalization already supplies the behavior boundary and another filesystem-shape test was not justified for this repair. | N/A | `29f81461` | NONE - symlink test intentionally not added |
| RTST-4 | test detector | dependency validation lacks a missing-reference row | Medium / High | dependency graph table; missing parent and non-Task dependency cases | VALIDATED | AUTO_FIX | Missing dependency IDs are a distinct pre-write graph rejection and need direct contract coverage. | N/A | `29f81461` | APPLIED - missing dependency reference regression passes |
| RSIM-1 | simplicity detector | shared-context and executable-descendant checks repeatedly walk parent chains | Medium / High | `has_ancestor_container`; per-Task ancestor accumulation; valid parent-tree invariant; deep fixture | VALIDATED | AUTO_FIX | One iterative hierarchy-facts pass can answer ancestor queries and executable-descendant checks without repeated O(containers*tasks) parent walks. | N/A | `29f81461` | APPLIED - one reusable span/postorder facts pass retains behavior; 25,000 nested containers plus 25,000 shared-context Tasks pass in the focused suite |

### Second review return

- **Validated:** 6 - RCON-1, RCON-2, RTST-1, RTST-2, RTST-4, RSIM-1.
- **Cleared (rejected):** 1 - RTST-3; no symlink test added.
- **Correctness detector:** no candidates.
- **Needs evidence:** 0.
- **Repairs:** reject NUL across plugin/verify argv; require only a nonblank program; retain literal tail arguments and cross-template-key verify placeholders; cover `none`, resumed shared rejection, and missing dependencies; precompute iterative hierarchy spans and executable-descendant facts once.
- **Coverage:** COMPLETE 4/4 detectors; fresh adjudicator complete; post-repair re-adjudication remains pending.
- **Applied verification:** `cargo test --test workflow_template_test` passed 7/7; `cargo clippy -- -D warnings`, `cargo fmt --check`, full `just check`, and `git diff HEAD --check` passed. Full tests emitted only the existing test-helper dead-code warnings.

### Gate 2 final attempt 2026-08-13

**Scope/code fingerprint:** `sha256:2d2cfaa207e51ef9de50b65ba2fc60df6833b70ea16af38aebec614baebeda9c`
**Task acceptance:** Current T1 / S1 workflow contracts and validation after both repair cycles
**Exact diff:** `git diff --cached` at the code/test fingerprint above
**Gate 1 evidence:** PASS - `cargo test`, `cargo clippy`, `cargo fmt --check`, `just check`, focused 7/7 tests, and `git diff --cached --check` exited 0
**Coverage:** detectors COMPLETE 4/4 · specialists none triggered · adjudicator COMPLETE (fresh)
**Review state:** REVIEWED

| candidate ID | source | location / claim | severity / confidence | evidence checked | disposition | action | rationale | duplicate_of | fingerprint | repair state |
|---|---|---|---|---|---|---|---|---|---|---|
| FTST-1 | test detector | malformed `--input` syntax and empty-key direct cases | Medium / Low | Decision-10; existing input parser and malformed input table | REJECTED | DROP | Straightforward branches of the already-covered input mechanism add little distinct confidence. | N/A | `2d2cfaa2` | none |
| FTST-2 | test detector | nested unknown-field cases | Low / Low | shared serde `deny_unknown_fields` mechanism and strict outer contract tests | REJECTED | DROP | Per-struct repetition would not cover a distinct mechanism. | N/A | `2d2cfaa2` | none |
| FTST-3 | test detector | initial shared request positive case | Medium / Low | valid resumed shared, missing-session rejection, and valid none cases | REJECTED | DROP | Existing cases pin the exact conditional contract; another positive row is redundant. | N/A | `2d2cfaa2` | none |
| FTST-4 | test detector | in-workspace `..` normalization case | Low / Low | canonical absolute output and canonical-target containment tests | REJECTED | DROP | The spelling variant adds negligible confidence beyond the security-relevant boundary tests. | N/A | `2d2cfaa2` | none |
| FTST-5 | test detector | error-variant matching in failure assertions | Low / Low | central `InvalidInput` constructor and user-facing message assertions | REJECTED | DROP | No current taxonomy defect or distinct behavior gap exists. | N/A | `2d2cfaa2` | none |
| FSIM-1 | simplicity detector | explicit digest-ready loader artifact | Low / Low | serializable resolved definition and T2 digest ownership | REJECTED | DROP | Duplicate reads are speculative; T2 can derive or extend digest material at its own boundary. | N/A | `2d2cfaa2` | none |

### Final review return

- **Validated:** 0.
- **Cleared (rejected/duplicate):** 6 rejected.
- **Needs evidence:** 0.
- **Repairs:** 0 remaining; both prior repair cycles are Gate-1 green.
- **Coverage:** COMPLETE 4/4; current T1 fingerprint REVIEWED.
