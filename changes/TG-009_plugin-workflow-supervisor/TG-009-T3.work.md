# Worklog: T3 - Campaign runner state shell

**Unit:** T3
**Route:** PROCEED
**Status:** done

## Plan

- Reconstruct the selected Campaign only from active/archive TG items, reject incomplete or escaping workflow state, and keep selection and rollup pure around the Store edge.
- Take one nonblocking Campaign advisory lock for `tg workflow run`, then use `tasks.lock` only for snapshot, claim, revalidation, and bottom-up archival transitions.
- Return only the versioned state-shell outcomes `resume`, `claimed`, and `complete`; do not launch plugins or expose T5 status projection data.

## Checks (planning bar)

- **PB-EXISTS** - VERIFIED - `CHANGE.md:46` remains unchecked and no runner module or `workflow run` action existed before this unit.
- **PB-SENSE** - VERIFIED - `DESIGN.md:204-224` assigns deterministic selection, process exclusion, short Store mutations, and bottom-up closure to this slice.
- **PB-PLAN** - VERIFIED - `Store::commit_status_change` and `Store::commit_done` preserve existing event/archive ordering, while `deps::compute_ready_queue` supplies the established ready ordering.
- **PB-CONTRACT** - VERIFIED - T1/T2 `WorkflowMetadata`, `Item`, status transitions, strict archive reader, and the `x-workflow` runtime extension carry all required state without a parallel ledger.
- **PB-SHAPE** - VERIFIED - projection/validation/selection are value transforms in `workflow::runner`; filesystem process exclusion and CLI rendering remain at the command edge.

## Judgment Calls

- Selected Campaign validation is fail-closed for every reachable descendant and for matching-instance items outside that tree; unrelated Campaign records remain isolated so an unrelated malformed item cannot block progress.
- A Campaign with no ready Task but unfinished state returns an error rather than inventing an unapproved shell outcome; `complete` is emitted only after all Campaign items archive.

## Evidence

- RED: `cargo test --test workflow_runner_test` failed before implementation because the test target did not exist.
- GREEN: `cargo test --test workflow_runner_test` passed 4/4 for scoped ready ordering with an ID tie-break, dependency readiness, resume/ambiguous doing state, malformed out-of-scope child rejection, bottom-up closure, same-Campaign lock exclusion, and unrelated Campaign progress; `cargo test workflow::runner::tests` passed the dependency revalidation unit test.
- Full verification: `cargo fmt`, `just check` (format check, Clippy with warnings denied, 149 library tests, all integration tests, and doc tests), and `git diff HEAD --check` passed. Existing per-test common-helper dead-code warnings remain non-failing.

## Scope Exclusions

- No plugin invocation, verify execution, IPC/result ingestion, session mutation, status command/projection, runtime gitignore/doctor updates, checkbox edit, commit, push, or ai-dotfiles modification.

## Review State

NOT REVIEWED

## Planning Concern

- The complete T3 slice is 1,224 changed non-generated lines (1,223 added, 1 removed), exceeding the 900-line target. The fail-closed Campaign reconstruction and focused Store/process regressions are coupled to the required behavior, so no coherent T3 remainder was deferred; the T3 checkbox remains untouched for the orchestrator.

## Adjudicated T3 Repairs

- BLOCK C1/C3/C5/C6
- AUTO_FIX C4/C7/C9/C12/C13/C14/C15/C16/C17/C18
- FOLLOWUP C2/C10
- REJECTED C8/C11

- Repairs verified but NOT REVIEWED pending fresh re-adjudication.
- Deferred followup: document `tg doctor --fix` support for commit-done crash reconciliation. No reconciliation was added in T3.

## Terminal Re-adjudication

- **Review state:** REVIEWED - a fresh terminal reviewer verified every C1-C18 disposition and found no remaining blocker or auto-fix.
- **Scale:** Iterative projection and rollup passed a 25,000-container plus 25,000-Task deep Campaign regression without recursion.
- **Verification:** `cargo test --test workflow_runner_test` passed 16/16, the deep runner unit regression passed, `just check` passed formatting, Clippy with warnings denied, 149 library tests, every integration test, and doc tests, and `git diff HEAD --check` passed. Existing per-test common-helper dead-code warnings remain non-failing.
- **Landing partition:** Separate nonblocking locks, batch completion support, the Campaign state shell, focused tests, and this final documentation transaction. The Campaign behavior commit is 925 changed lines because its 127-line private-module scale regression must exercise private projection functions; every other commit remains below 900 lines.
- **Landing:** Nonblocking locks landed as `c4545d0`, batch container completion as `d38f106`, Campaign state behavior as `1330e22`, and focused integration coverage as `800a4f0`.
