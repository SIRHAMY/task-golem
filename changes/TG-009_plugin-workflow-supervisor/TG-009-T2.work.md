# Worklog: T2 - Workflow graph instantiation

**Unit:** T2
**Route:** PROCEED
**Status:** done

## Plan

- Derive a canonical SHA-256 digest from the validated definition's resolved semantic material, including the exact input map and resolved plugin contracts while excluding machine-local source paths.
- Convert the validated nodes into ordinary TG items after allocating every ID in memory, preserving parent and dependency mappings and embedding the complete `x-workflow` projection.
- Keep definition loading, validation, and digest preparation outside the Store lock; inside one lock, return a matching active/archive instance or append the complete graph with one atomic active-store write.
- Add only the `tg workflow instantiate` route and deterministic Campaign/node-map output needed to exercise T2; defer run and status behavior.

## Checks (planning bar)

- **PB-EXISTS** (problem still exists) - VERIFIED - `src/workflow.rs:1` exposes only T1 template contracts, while `src/cli/args.rs:18-267` and `src/cli/commands/mod.rs:1-16` have no workflow instantiation route.
- **PB-SENSE** (still makes sense) - VERIFIED - T2 remains unchecked at `CHANGE.md:45`, T1 is landed, and S1 still lacks any persisted Campaign graph or instance idempotency.
- **PB-PLAN** (plan achieves impact, in bounds) - VERIFIED - `Store::with_lock` and `Store::save_active` at `src/store/mod.rs:82-100` provide the required short lock and atomic batch rewrite; `Item` parent, dependencies, and `x-` extensions at `src/model/item.rs:43-75` carry the validated graph without a second store.
- **PB-CONTRACT** (contract/types still sound) - VERIFIED - T1's `WorkflowDefinition` is the validation success value at `src/workflow/template.rs:57-64,135-161`; a typed serializable `x-workflow` projection and deterministic result map can encode every Decision-3 field while reusing `TgError::InvalidInput` for expected instance conflicts.
- **PB-SHAPE** (service shape sound) - VERIFIED - loading and canonical digest construction remain value preparation at the edge, item construction is a pure whole-graph transform, and the Store is passed explicitly to one batch instantiation operation whose only mutation is a single locked save.

## Judgment calls

- What belongs in the canonical digest? -> Hash canonical JSON for resolved version/name/nodes, exact inputs, and resolved plugin version/argv, excluding absolute template/plugin source paths (high confidence) - filesystem locations establish the trusted loading boundary but are not workflow execution material, so excluding them keeps an unchanged workflow idempotent when the workspace moves while satisfying Decision-10.

## Where it left off

- T2 implementation, focused integration coverage, and minimal instantiate CLI routing are complete; the Task checkbox remains unchanged for orchestrator-owned progress.

## Prevention / diff self-check

- RED: `cargo test --test workflow_instantiate_test` failed 0/4 before implementation because clap rejected the missing `workflow` subcommand, pinning the intended CLI and persistence boundary.
- GREEN: `cargo test --test workflow_instantiate_test` passed 4/4 for complete metadata/edge projection, deterministic active and archived idempotency, input/plugin digest mismatch no-write behavior, and pre-write validation; the existing `workflow_template_test` passed 7/7.
- Full verify: `just check` passed formatting, Clippy with warnings denied, 148 library tests, all CLI/integration tests, and doc tests; only the repository's existing per-test common-helper dead-code warnings remain.
- Commit partition verify: a detached-worktree `just check` passed for the 444-line feature/dependency slice, and the final `just check` passed with the 294-line tests/worklog slice applied; `GREEN_PER_COMMIT: yes` for the suggested two-commit partition (generated lockfile excluded from size counts).
- Exact diff review: scope is limited to SHA-256 digest material, one Store-backed batch instantiation capability, minimal `workflow instantiate` CLI/output, focused tests, dependency lockfile, and this worklog. No runner/status behavior, process execution, runtime session mutation, source TODO/FIXME, debug output, or Task checkbox edit was added.
- Simplicity/safety: validation and canonical digest preparation occur before `Store::with_lock`; the lock performs active/archive idempotency resolution, in-memory whole-graph construction, and at most one atomic `save_active`. Output and node maps use `BTreeMap`; all new source comments are clap help contracts, while test comments are required Arrange/Act/Assert or journey checkpoints.

## Review ledger

### Gate 2 adjudicated repair return 2026-08-13

**Scope/code fingerprint:** `sha256:7a1db705b6cfbc9d240dfb394572f2d1a94485aa98c742c7149607fe5d7d4238` (pre-repair staged T2 code/tests)
**Post-repair code fingerprint:** `sha256:d2439d0c01f33c4064cdca93a38d6441e559db6917b7de4efaaff8d5ead7df1b` (corrected staged T2 code/tests)
**Task acceptance:** `CHANGE.md` T2 / S1 exact validated-graph instantiation, complete `x-workflow` projection, one locked write, and active/archive instance-key idempotency
**Exact diff:** `git diff --cached -- Cargo.toml Cargo.lock src/cli/args.rs src/cli/commands/mod.rs src/cli/commands/workflow.rs src/cli/mod.rs src/workflow.rs src/workflow/instantiate.rs src/workflow/template.rs tests/workflow_instantiate_test.rs` at the fingerprint above
**Post-repair exact diff:** the same T2 path set plus `src/store/jsonl.rs` and `src/store/mod.rs` at the post-repair fingerprint above
**Gate 1 evidence:** PASS - the staged unit's worklog records focused instantiation/template tests, full `just check`, and exact diff review before Gate 2
**Coverage:** adjudicated repair handoff source-complete below; validated repairs only, with duplicates and rejected scope retained
**Review state:** NOT REVIEWED - repairs are applied locally and require fresh re-adjudication

| candidate ID | source | location / claim | severity / confidence | evidence checked | disposition | action | rationale | duplicate_of | fingerprint | repair state |
|---|---|---|---|---|---|---|---|---|---|---|
| T2-COR-1 | correctness review | `find_existing_instance`: a matching digest returns a partial, extra, or edge-altered persisted graph | High / High | T2 exact-graph acceptance; persisted node identity, parent, dependency, and Campaign mapping | VALIDATED | BLOCK | Digest equality identifies the expected definition but does not prove the persisted records still encode it; idempotency must reject graph drift before any write. | N/A | `7a1db705` | APPLIED - exact node set, version/instance/node/kind identity, parent edges, and dependency edges are compared with regressions for missing, extra, identity-altered, and edge-altered records |
| T2-COR-2 | correctness review | `instantiate_workflow`: tolerant archive loading can skip the record reserving an instance key and create a duplicate graph | High / High | `jsonl::read_archive` skip-and-warn behavior; uniqueness check and lock/write sequence | VALIDATED | BLOCK | Uniqueness-sensitive instantiation needs a complete archive view and must fail closed when any archive record cannot be trusted. | N/A | `7a1db705` | APPLIED - instantiation uses a strict archive read while ordinary archive APIs retain skip-and-warn behavior; malformed-record no-write regression passes |
| T2-TST-1 | test review | same-key concurrent instantiation lacks process-level regression coverage | High / High | Store lock boundary; existing sequential idempotency test | VALIDATED | AUTO_FIX | Two processes must both return one mapping while only one four-node graph is persisted. | N/A | `7a1db705` | APPLIED - concurrent process regression asserts two successes, identical results, and one graph |
| T2-TST-2 | test review | graph-definition changes are not directly shown to change the digest and avoid writes | Medium / High | existing input/plugin mismatch checks; digest material includes resolved nodes | VALIDATED | AUTO_FIX | Graph material is a distinct digest input and needs a direct dependency-edge change regression. | N/A | `7a1db705` | APPLIED - changed graph rejects the key with a different digest and byte-stable active store |
| T2-TST-3 | test review | multi-input CLI argument order is not proven digest-invariant | Medium / High | `BTreeMap` input normalization; digest serialization | VALIDATED | AUTO_FIX | Semantically identical input maps must not depend on argument order. | N/A | `7a1db705` | APPLIED - reversed two-input order returns the same instance and digest without another graph |
| T2-TST-4 | test review | add malformed-archive uniqueness regression | High / High | T2-COR-2 evidence and proposed strict reader | DUPLICATE | NONE | This is the regression manifestation of T2-COR-2 and is repaired by the same behavior change. | T2-COR-2 | `7a1db705` | covered by T2-COR-2 repair |
| T2-TST-5 | test review | add persisted node-set/identity/edge drift regressions | High / High | T2-COR-1 evidence and persisted item mutation cases | DUPLICATE | NONE | These cases prove the T2-COR-1 repair rather than define a separate behavior defect. | T2-COR-1 | `7a1db705` | covered by T2-COR-1 repair |
| T2-CON-1 | contract review | extend root discovery or CLI behavior for nested invocation CWD | Medium / Medium | T2 checklist; T6 nested-CWD acceptance; explicit repair scope | REJECTED | DROP | Nested-CWD behavior belongs to T6 and was explicitly excluded from this repair. | N/A | `7a1db705` | NONE - no nested-CWD changes added |
| T2-CON-2 | contract review | add container `session_ref` projection or mutation support | Medium / High | `DESIGN.md` runtime projection; T4 session ownership; T2 checklist | REJECTED | DROP | Session references are runtime state owned by T4, not graph-instantiation state in T2, and were explicitly excluded. | N/A | `7a1db705` | NONE - no `session_ref` changes added |
| T2-SIM-1 | simplicity review | make the ordinary archive reader globally fail-fast | Medium / High | established `read_archive` skip-and-warn contract; uniqueness-only need | REJECTED | DROP | Changing all archive consumers would alter shipped recovery behavior; the strict path is limited to uniqueness-sensitive instantiation. | N/A | `7a1db705` | NONE - ordinary archive behavior remains unchanged |

### Review return

- **Validated:** 5 - T2-COR-1, T2-COR-2, T2-TST-1, T2-TST-2, T2-TST-3.
- **Cleared (rejected/duplicate):** 5 - T2-CON-1, T2-CON-2, T2-SIM-1 rejected; T2-TST-4 and T2-TST-5 deduplicated.
- **Needs evidence:** 0.
- **Repairs:** exact persisted-graph comparison; strict archive read only for uniqueness-sensitive instantiation; concurrent same-key, graph-digest drift, input-order invariance, graph-tamper, and malformed-archive regressions.
- **Scope exclusions:** no nested-CWD, `session_ref`, runner, status, or Task-checkbox changes.
- **Applied verification:** `cargo test --test workflow_instantiate_test` passed 9/9; `cargo test --test workflow_template_test` passed 7/7; final `just check` passed formatting, Clippy with warnings denied, 148 library tests, every integration test, and doc tests; `git diff HEAD --check` and staged diff checks passed. Only the repository's existing per-test common-helper dead-code warnings were emitted.

### Gate 2 second-cycle adjudicated repair return 2026-08-13

**Scope/code fingerprint:** `sha256:d2439d0c01f33c4064cdca93a38d6441e559db6917b7de4efaaff8d5ead7df1b` (pre-repair staged T2 code/tests)
**Post-repair code fingerprint:** `sha256:0fc060ce47a895aec4fa90fae64c635cdf590bc1cfb3354c8ab38e9de0fafa49` (corrected staged T2 code/tests)
**Task acceptance:** `CHANGE.md` T2 / S1 injective node mapping, exact immutable persisted projection, one locked write, and fail-closed active/archive instance-key idempotency
**Exact diff:** `git diff --cached -- Cargo.toml Cargo.lock src/cli/args.rs src/cli/commands/mod.rs src/cli/commands/workflow.rs src/cli/mod.rs src/workflow.rs src/workflow/instantiate.rs src/workflow/template.rs src/store/jsonl.rs src/store/mod.rs tests/workflow_instantiate_test.rs` at both fingerprints above
**Gate 1 evidence:** PASS - `cargo test --test workflow_instantiate_test` passed 12/12; `just check` passed formatting, Clippy with warnings denied, 148 library tests, all integration tests, and doc tests; staged diff check passed
**Coverage:** supplied second-cycle adjudication is source-complete below; fresh post-repair re-adjudication remains pending
**Review state:** NOT REVIEWED - every validated repair is staged and verified at the post-repair fingerprint, pending fresh re-adjudication

| candidate ID | source | location / claim | severity / confidence | evidence checked | disposition | action | rationale | duplicate_of | fingerprint | repair state |
|---|---|---|---|---|---|---|---|---|---|---|
| T2-R2-COR-1 | correctness review | `find_existing_instance`: two workflow nodes can resolve to the same persisted item ID | High / High | returned node map contract; duplicate active IDs remain readable; exact graph identity | VALIDATED | BLOCK | A node-to-item map must be injective or the returned Campaign graph aliases distinct nodes and cannot represent the validated definition. | N/A | `d2439d0c` | APPLIED and VERIFIED - duplicate mapped item IDs fail as storage corruption before any write |
| T2-R2-COR-2 | correctness review | `find_existing_instance`: title, description, plugin, context, input, and verify drift is accepted | High / High | T2 projection in `CHANGE.md` Decision-3; `build_items`; existing identity and edge checks | VALIDATED | BLOCK | Digest equality identifies the expected source definition but idempotency must also prove every immutable persisted T2 field still matches it exactly. | N/A | `d2439d0c` | APPLIED and VERIFIED - item content and resolved execution projection are compared field-for-field with per-field no-write regressions |
| T2-R2-COR-3 | correctness review | `read_archive_strict`: missing and zero-byte archives are treated as complete empty history | High / High | uniqueness-sensitive strict call; header schema contract; malformed-record fail-closed behavior | VALIDATED | BLOCK | An absent or headerless archive cannot prove instance-key uniqueness and must fail closed, while a valid schema header alone is a complete empty archive. | N/A | `d2439d0c` | APPLIED and VERIFIED - missing and zero-byte archives reject; a valid header-only archive succeeds |
| T2-R2-TST-1 | test review | repaired rejection paths do not uniformly prove both stores remain unchanged | Medium / High | graph tamper helper; malformed archive regression; missing/empty archive states | VALIDATED | AUTO_FIX | Failure before instantiation must preserve active and archive bytes or absence, not merely return an error. | N/A | `d2439d0c` | APPLIED and VERIFIED - graph, malformed archive, missing archive, and empty archive regressions assert no writes |
| T2-R2-CON-1 | contract review | immutable comparison could reject runtime TG state or future container `session_ref` | High / High | Design TG projection/runtime ownership; T2/T4 boundary; metadata deserialization | VALIDATED | BLOCK | Status/claim fields and container session references are mutable runtime state, so they cannot invalidate a matching T2 instantiation. | N/A | `d2439d0c` | APPLIED and VERIFIED - runtime item state and unknown future `session_ref` are excluded and remain byte-stable on repeat instantiation |
| T2-R2-SIM-1 | scope review | include pruned archive history in instance-key discovery | Medium / High | explicit second-cycle scope; T2 active/archive acceptance | REJECTED | DROP | Pruned archive behavior was explicitly excluded and is not required for the requested active/archive strictness repair. | N/A | `d2439d0c` | NONE - no pruned archive changes |
| T2-R2-SIM-2 | simplicity review | refactor active/archive strict readers while tightening archive validity | Low / High | existing reader contracts; uniqueness-only strict path; requested minimal repair | REJECTED | DROP | Consolidating readers would broaden the diff without changing the requested T2 behavior and risks altering established tolerant archive semantics. | N/A | `d2439d0c` | NONE - strict readers remain structurally unchanged |

### Second-cycle review return

- **Validated:** 5 - T2-R2-COR-1, T2-R2-COR-2, T2-R2-COR-3, T2-R2-TST-1, T2-R2-CON-1.
- **Cleared (rejected/duplicate):** 2 - T2-R2-SIM-1 and T2-R2-SIM-2 rejected; no duplicates.
- **Needs evidence:** 0.
- **Repairs:** reject aliased node/item IDs; compare the complete immutable T2 persisted projection; ignore runtime state and future `session_ref`; reject missing/empty strict archives; preserve valid header-only archives; prove no-write failures.
- **Scope exclusions:** no pruned archive behavior, strict-reader refactor, runner/status behavior, plan edits, Task-checkbox changes, or commit.
- **Applied verification:** focused T2 integration tests passed 12/12; `just check` passed; staged diff check passed. Only the repository's existing per-test common-helper dead-code warnings were emitted.
- **Coverage:** supplied second-cycle candidates and explicit exclusions are fully dispositioned; fresh post-repair re-adjudication remains pending, so this fingerprint is not claimed `REVIEWED`.

### Gate 2 third-cycle adjudicated repair return 2026-08-14

- **C1:** BLOCK repaired - all `x-workflow` records are parsed before instance filtering.
- **C2:** BLOCK repaired - duplicate item IDs across active/archive records fail before return or write.
- **C3:** AUTO_FIX repaired - concurrent instantiation uses a readiness/release barrier.
- **C4:** AUTO_FIX repaired - every created item has exact default-state assertions.
- **C5:** REJECTED - no change applied.
- **C6:** AUTO_FIX repaired - every fixture node has an exact `x-workflow` JSON assertion.
- **C7:** AUTO_FIX repaired - human output covers Campaign, instance, digest, and sorted nodes.
- **C8:** REJECTED - no change applied.
- **C9:** BLOCK repaired - only expected shared-context owner containers may carry `session_ref`.
- **Review state:** post-repair review is pending fresh re-adjudication; `CHANGE.md` T2 remains unchecked.

### Terminal re-adjudication 2026-08-14

- **Review state:** REVIEWED - a fresh terminal reviewer verified C1-C9 against the complete repaired diff and found no remaining blocker or auto-fix.
- **Final repair:** Added the one missing initial-state assertion that every instantiated item has an empty tag set.
- **Verification:** `cargo test --test workflow_template_test --test workflow_instantiate_test` passed 23/23; isolated production commit `just check` passed; final `just check` passed formatting, Clippy with warnings denied, 148 library tests, every integration test, and doc tests. Only the repository's existing per-test common-helper dead-code warnings remain.
- **Landing:** production behavior landed as `7393bd7`; integration coverage landed as `7a82294`; the orchestrator marked T2 complete in this final documentation transaction.
